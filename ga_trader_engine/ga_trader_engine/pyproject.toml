[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ga_trader"
version = "0.1.0"
description = "GA-based trading strategy search with Pine export"
requires-python = ">=3.10"
